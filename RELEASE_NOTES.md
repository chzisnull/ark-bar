## 🚀 ArkBar v0.6.1 更新说明

### 🌟 本次版本更新内容

1. **火山方舟「检测没问题，但就是取不到用量」——修好了**：
   - **根因**：`arkcli auth status` 只说明「本地存着凭据」，凭据本身可能已经失效。SSO 的 `refresh_token` 过期后它照样回 `logged_in: true`，于是引导页说已就绪、界面说没问题，真去取用量却报 `requires Volcengine Ark SSO STS` / `refresh_token is invalid`。
   - **环境检测改成真核实**：报「已登录」之后再用一次真实调用确认；核实不过就按未登录处理，并写明「火山方舟登录已失效（SSO 凭据无法续期），请重新授权登录」，引导页的授权步骤随之重新亮起。
   - **授权失效与取数失败分开报**：识别 SSO STS / refresh_token / token 交换失败这类错误，直接说「登录已失效，需要重新授权」，按钮也变成「重新授权」，不再只丢一句「获取配额失败」。
   - **不再拿旧读数盖住它**：以前凭据失效时缓存会保留上一次「已连接」的数据、只标一句「同步失败」，界面看起来一切正常。现在「没装 CLI / 没登录 / 凭据失效」这类用户能自己解决的问题会**当场替换并显示**；只有网络/服务端抖动才保留旧数据。
   - 卡片里只展示人话（状态行 + 一行截断的原始报错），整段 JSON 不再把卡片撑成一大块。

2. **外接屏选「所有显示器」却只有主屏有刘海——修好了**：
   - **根因**：新建的刘海窗口是按 `visible: false` 建出来的，只有启动和托盘那条路径会把它显示出来。于是在设置里把范围切到「所有显示器」之后，第二块屏上的窗口其实建好了，却一直藏着。
   - 现在对账时会看舰队当前是不是显示态：是就把新窗口一并显示出来；热插拔显示器时同样如此。

3. **新增落盘日志，方便报障**：
   - 关键路径（舰队对账、每个刘海的摆放、点穿状态、活动状态变化）都会写进 `~/Library/Application Support/com.chzisnull.arkbar/arkbar.log`（Windows 对应 AppData 下同一路径），超过 512 KB 只留尾部。
   - 以前 macOS 从 Finder 启动时 stderr 无处可去，「刘海不在这块屏上」「点了没反应」这类问题只能靠猜。现在把日志发回来即可定位。
