## 🚀 ArkBar v__VERSION__ 更新说明

### 🌟 本次版本更新内容
1. **多模型服务商集成 (Multi-Provider Monitoring)**：
   - ⚡ **xAI Grok**：支持读取本地授权状态，监控 SuperGrok 订阅池总用量及 Grok3、Mini、Heavy 等各子模型消耗占比与周重置倒计时。
   - 🌐 **Google Antigravity**：深度集成 agy CLI，实时监测 Gemini 5小时会话池与周度配额余量及重置时间。
   - 🤖 **OpenAI Codex**：自动识别 `~/.codex/auth.json` 凭证与本地配额快照，精准追踪 5小时与 7天额度池。
   - 🌋 **火山方舟 (Volcengine)**：Coding Plan / Agent Plan 配额监控持续稳定提供支持。

2. **桌面悬浮监控框全面升级 (Floating Widget)**：
   - **默认首选厂商自定义**：支持在设置面板中指定悬浮框常驻展示的默认厂商（如 Grok、火山方舟等）。
   - **鼠标悬停动态看板**：鼠标悬停在悬浮窗上方时，卡片平滑动态展开，一次性清晰展示当前所有已授权厂商的最新用量、进度条与重置周期；移开鼠标自动缩回 44px 微型胶囊。
   - **悬浮窗拖拽交互稳定化**：消除 Windows / macOS 跨屏高分屏拖拽位移抖动问题，拖拽时锁定胶囊卡片防止闪烁。

3. **极速 0ms 厂商切换与数据预热 (0ms Instant Switch)**：
   - **前端本地持久化即时渲染**：切换不同厂商 Tab 时实现 0 延迟即刻呈现数据，告别骨架屏和旋转等待。
   - **智能静默预载（Silent Prefetch）**：系统后台自动并发预取各厂商配额，无需用户手动逐一等待。
   - **Rust 后端内存级短时缓存（TTL 60~90s）**：规避频繁调用外部子进程造成的资源开销与界面卡死。

---

### 🛠️ 修复的问题
1. **修复网页授权时窗口遮挡且无法收起问题**：
   - 主窗口取消强制顶置属性（`alwaysOnTop: false`），新增全局失焦（Blur）自动收起逻辑。
   - 点击浏览器进行第三方授权时，主窗口自动退至后台，授权完毕后点击托盘即可快速唤起。
2. **精简优化服务商接入流程**：
   - 废弃手动粘贴 Token 的冗余表单，全面改为自动识别本地已有凭证，开箱即用。
3. **修复悬浮框右键菜单与退出交互**：
   - 支持在悬浮卡片上右键呼出快捷操作菜单，支持一键切换主窗口、一键退出应用。

---

### ⚠️ macOS 首次打开提示“应用已损坏，无法打开”解决办法
由于开源项目未购买苹果商业开发者证书，从浏览器下载后 macOS Gatekeeper 会自动对应用添加隔离标记。  
**解决方法**：打开系统自带「终端」(Terminal.app)，复制并执行以下命令回车即可：
```bash
xattr -dr com.apple.quarantine /Applications/ArkBar.app
```

---

### 📦 安装包下载指引
- **macOS (Apple Silicon M1/M2/M3/M4 系列)**: `ArkBar_*_aarch64.dmg`
- **macOS (Intel 处理器)**: `ArkBar_*_x64.dmg`
- **Windows (64位安装包)**: `ArkBar_*_x64-setup.exe` (或 `.msi`)
