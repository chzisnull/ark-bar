## 🚀 ArkBar v0.6.2 更新说明

### 🌟 本次版本更新内容

1. **有新版本会主动提示，不用再自己去点「检查更新」**：
   - **后台自动巡检**：启动 15 秒后查一次，之后每 6 小时一次。发现新版本会广播给所有窗口（同一个版本一个会话只提示一次，不反复打扰），并写进 `arkbar.log`。
   - **刘海上的提示**：设置齿轮上亮一个琥珀色小圆点；展开刘海时卡片里直接出现一行「有新版本 vX.Y.Z 可用」+「去更新」按钮，点一下就把设置窗口和更新弹窗一起带出来。
   - **设置窗口**：收到广播即记住新版本信息，打开设置就能看到更新入口。
   - **可关闭**：设置 → 通用新增「自动检查更新」开关（默认开）。关掉之后后台巡检真的不再发起请求，只在你手动点击时检查——与 Codenotch 的 `automatic` 偏好同一个语义。
   - 说明：提示只在**确实存在更新**时出现，所以刚升级到最新版的用户不会看到它；等下一个版本发布时自然就来了。

### 🔧 一并带上的修复

- 上一版（v0.6.1）的两处修复同样包含在本版：火山方舟凭据失效时如实报「需要重新授权」（不再出现「检测没问题但取不到用量」），以及多屏选「所有显示器」却只有主屏有刘海。
