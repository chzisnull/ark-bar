## 🚀 ArkBar v__VERSION__ 更新说明

### 🌟 本次版本优化与修复

1. **检查更新极致性能优化（零卡顿）**：
   - 将检查更新重构为异步非阻塞任务（`spawn_blocking`），彻底解放 IPC 线程，杜绝检查更新时的界面掉帧与卡顿。
   - 增加 **GitHub 302 极速快查通道**（仅拉取几百字节 Header，无需等待大体积 JSON 响应，绕过 GitHub 60次/小时 API 限制），无新版本时毫秒级即刻返回。
   - 增加 10 分钟内存级防抖缓存，避免重复发起后台网络请求。

2. **解决点击检查更新异常“弹回顶部”问题**：
   - 彻底移除原生 HTML `disabled` 属性导致的 WebKit 焦点重置与强行归零 `scrollTop` 问题，改用无焦点副作用的状态锁定。
   - 增加滚动坐标记忆锚定与平滑状态反馈，检查完毕原地展示结果，绝不再发生布局抖动或页面瞬移。

3. **新版本置顶呈现与全局联动**：
   - 设置页一旦检测到新版本，在页面最顶部常驻醒目的渐变高亮横幅，提供「网页下载」与「立即在线更新」操作，无需翻至底部。
   - 设置页顶部版本号区域增加新版动态徽章。
   - 检查结果即时同步至主面板，主面板顶部通栏联动展示新版本横幅，右上角设置齿轮出现动态呼吸红点。

---

### 🌟 0.3.x 重磅核心特性（回顾）
- **多厂商 Token 详细用量看板**：全面支持近 5 小时、今日、本周、本月 Token 消耗统计，Prompt/Completion/Cache 命中拆解。
- **历史明细报表与数据导出**：过去 14~30 天历史柱状走势图、按日明细表格及 JSON 一键导出。
- **主面板双模切换**：顶部胶囊一键切换「⚡ 配额监控」与「📊 Token 统计」。
- **状态栏托盘增强**：支持显示今日 Token（如 `🔥 602K`）、5小时 Token、配额百分比等多种模式。

---

### ⚠️ macOS 首次打开提示“应用已损坏，无法打开”解决办法
由于开源项目未购买苹果商业开发者证书，从浏览器下载后 macOS Gatekeeper 会自动对应用添加隔离标记。  
**解决方法**：打开系统自带「终端」(Terminal.app)，复制并执行以下命令回车即可：
```bash
xattr -dr com.apple.quarantine /Applications/ArkBar.app
```

---

### 📦 安装包下载指引
- **macOS (Apple Silicon M1/M2/M3/M4 系列)**: `ArkBar_*_aarch64.dmg`
- **macOS (Intel 处理器)**: `ArkBar_*_x64.dmg`
- **Windows (64位安装包)**: `ArkBar_*_x64-setup.exe`（或 `.msi`）
