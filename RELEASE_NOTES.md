## 🚀 ArkBar v0.5.0 更新说明

### 🌟 本次版本更新内容

1. **贴边摆放与拖动：完全按 Codenotch 重写（修复刘海会跑到屏幕中间）**：
   - **拖动不再把刘海甩出边缘**：之前按住抓手是按鼠标位移直接挪窗口，实测能把刘海从右边缘甩到屏幕中间（偏出 749pt），还会大幅过冲。现在由 Rust 原生线程跟随系统光标，**只沿当前这条边滑动**，位置由「沿边比例 + 窗口目标尺寸」算出并夹取，刘海永远焊在边缘上。
   - **抓手 = 搬运到任意边缘**：按住抓手会升起四条边的落区（半透明黑底 + 虚线轮廓，瞄准的那条更亮），指针挑一条边、松手即落。搬运期间刘海原地不动——选的是屏幕上的一个地方，不是移动了多远。
   - **⌥ + 拖动刘海本体 = 沿当前边微调**：与 Codenotch 一致；不带 ⌥ 的按下只是一次点击，手滑不会再误搬刘海。
   - **顶部贴边终于贴住屏幕顶边**：修掉两个真实成因——位置按「目标尺寸」而非实测尺寸计算，以及窗口层级必须是 `statusBar`（`alwaysOnTop` 的 `floating` 层级会被 AppKit 压回菜单栏下方，永远差 30pt）。
   - **每条边各自记住落点**：拖动落点按边分别持久化（写进 `notch.json`，不依赖 webview 存储），切回来原样恢复；「复位居中」只影响当前这条边。

2. **工作状态不再造假**：
   - 卡片底部那行状态以前是**写死的**「工作中 · 刚刚」+ 永远转圈的图标，所以不在工作时也显示在工作。现在按 Codenotch 的 activity 引擎真实探测：**Antigravity** 解析 `transcript.jsonl` 尾部的回合状态（含 `ask_question` → 等待回答、`RequestFeedback` → 等待确认，回合结束 9s 内显示已完成）；**Codex** 按 rollout 最后一步的类型决定安静容忍（工具 ≤10 分钟、思考 ≤120s、助手消息 ≤4s）；**Grok** 看 `updates.jsonl` 新鲜度并核对回合是否真的还开着。
   - 火山方舟 / Teamo 本地没有回合信号，**不显示状态**——宁可不报，也不编一个「工作中」。等待输入琥珀色、已完成绿色、工作中白色转圈。

3. **菜单栏图标终于能关掉**：
   - 设置里关掉后重启又会冒出来，是因为启动时读的是 v0.2.12 的老键 `arkbar_tray_icon_visible`，而设置面板写的是 `arkbar_show_tray`——两个键对不上，启动恒判为「显示」。现在统一到单一键（老键只做一次性迁移），关掉就是关掉。

4. **右键菜单**：刘海上右键即可「立即刷新 / 保持展开（勾选）/ 偏好设置 / 退出」，与 Codenotch 一致；菜单打开期间刘海不会在菜单底下收起来。

5. **Windows 上火山方舟刷不出来：定位并修复**：
   - `cmd /C` 调 arkcli 时程序路径没有加引号，**路径里只要有空格**（`C:\Users\First Last\AppData\Roaming\npm\arkcli.cmd`）就永远执行失败，用量自然一直空着。现在按 cmd 的规则正确加引号（`raw_arg`，避免 std 二次转义）。
   - `~/.arkcli/config.yaml` 的套餐档位在 Windows 上读不到（只认 `HOME`），补上 `USERPROFILE`。
   - **没装 CLI / 没授权时把话说清楚**：刘海卡片会直接写出原因（「未检测到 arkcli 命令行工具」/「未登录火山方舟或登录已过期」），并给一个「安装 / 授权」按钮，一键跳到引导页完成 npm 安装或 SSO / 授权码登录；未连接的厂商读数用琥珀色标出，不再是看起来像「0%」的灰。

6. **其余对齐与修复**：
   - 顶部贴边的窗口高度 480 → 650（上游方形尺寸，480 会把内容多一点的卡片顶出窗口）。
   - 详情卡片消失延迟 180ms → 250ms（上游值）。
   - 圆环悬停不再放大（指针一靠近就「粘」上去），改为**按下缩到 .93** 给回执；**点击圆环立即刷新**该厂商。
   - 收起胶囊的唤醒热区与页面自己的唤醒判定统一到同一组常量（此前 65px 与 50px 两套数字对不上）。
   - 换边后页面布局自动翻转（此前 Rust 广播的是 `notch_edge`，页面却在听 `notch_edge_changed`，事件名对不上，换边后布局留在原地）。
   - 摆放与点穿状态都有诊断日志（`notch placed` / `click-through`），出问题时不必再猜。
