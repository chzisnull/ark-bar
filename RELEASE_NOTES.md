## 🚀 ArkBar v0.6.0 更新说明

### 🌟 本次版本更新内容

1. **每块屏幕一个刘海（对齐 Codenotch 的 `NotchFleet`）**：
   - 设置 → 外观新增「刘海显示范围」：**主显示器**（默认，只有带菜单栏那一块，与单面板时代一致）或**所有显示器**（每块屏各一个刘海）。与上游同样的取舍——默认只留主屏，免得全新安装时把刘海撒到用户没预期的地方。
   - **每块屏的刘海各开各的**：悬停哪块屏就只展开哪块（`notch_pointer` / `notch_cursor` 改为只发给被指到的那一个窗口），不会再出现「碰一块屏、所有屏都弹开」。
   - **整队共享的仍然是共享的**：贴边位置、沿边落点、显示方式、厂商数据都是整队一份——一块屏上的刘海换边或沿边微调，其余屏幕一起跟着走（上游 `apply(edge:)` / `apply(alongOffset:)` 的扇出语义）。
   - **显示器插拔自动对账**：插上就补一个刘海、拔掉就收掉那一个，位置按「第 i 个窗口 ↔ 第 i 块屏」稳定对应（主屏永远排第一，其余按坐标排序）。
   - 托盘/快捷键的显隐改成整队开关：一块屏一个刘海，一起显、一起隐。
   - 覆盖范围存在 `notch.json`（与贴边位置、沿边落点同一份状态），重启后照旧。

### 🔧 一并带上的修复

- 上一版（v0.5.0）里已修的问题同样包含在本版：拖动不再把刘海甩出边缘、抓手=搬运到任意边缘（含四条落区提示）、⌥+拖动=沿边微调、顶部贴边严丝合缝、工作状态真实探测、菜单栏图标能关掉、Windows 上火山方舟能刷出来（含未装 CLI / 未授权时的明确提示与一键授权）。
