## 🚀 ArkBar v0.4.0 更新说明

### 🌟 本次版本核心更新：参考 Codenotch 全面重构 UI 视觉与交互动效

1. **全新屏幕边缘“灵动刘海”挂件 (Screen Edge Notch)**：
   - 贴靠屏幕边缘的 Apple 有机黑透曲面悬浮岛，支持右侧/左侧贴靠与闲置自动半隐。
   - 垂直排列各 AI 厂商项，每项配备**环形刻度仪表盘 (Circular Ring Gauge)**，SVG 进度弧线采用弹簧阻尼动画，依水位动态三色变色，圆环下方直观显示加粗百分比或余额（如 `68%`、`64%`、`$4.67`）。
   - 鼠标悬停即刻向外飞出**气泡卡片 (Speech Bubble Popover)**，指针尖角精准对齐当前图标，详细展示模型组配额进度、倒计时与实时工作状态。
   - 配备抓手支持沿屏幕边缘自如上下拖拽定位。

2. **全新 Codenotch 风格“设置与多账号管理中心”**：
   - 采用经典 macOS 两栏式窗口设计，左侧提供红绿灯按钮、品牌与分类导航（账号、外观、通知、通用）。
   - **账号面板**：支持拖拽抓手任意调整刘海绘制顺序，提供一键直达官方控制台胶囊按钮、iOS 风格纯蓝滑动开关，以及细粒度的「刘海显示」与「模型数据」选择。
   - **外观面板**：支持配置刘海屏幕贴靠位置（右侧/左侧/隐藏仅托盘）、闲置自动半隐与一键召唤桌面刘海浮窗。
   - **通用与通知**：高水位与重置系统推送、开机自启、多档后台刷新周期与在线自动更新。

3. **全局矢量资产与 Apple 弹簧物理缓动**：
   - 新增独立矢量图标组件 `ProviderIcon.vue`，重塑 Antigravity、Grok、火山方舟、Codex、Teamo 等图标。
   - 菜单栏弹窗与桌面刘海双向无缝联动。

---

## 🚀 ArkBar v0.3.7 更新说明

### 🛠 v0.3.7 修复：TeamoRouter 余额与今日费用不再显示 `--`
- 修复「账户余额」「今日费用」两张卡片始终显示 `--` 的问题。
  - 根因：TeamoRouter 现网接口的金额是**对象**形态 `{"value":"4.67380993","currency":"USD"}`，而解析逻辑只接受纯字符串，导致金额恒为空值。
  - 现在同时兼容对象 / 字符串 / 数字三种形态，并优先采用接口返回的 `currency`（缺省 `USD`）。
- 补强测试：新增对象形态、数字形态与异常载荷用例；带 `TEAMO_TEST_TOKEN` 的联网用例现在会断言余额与今日费用**确实解析成功**，防止同类静默回归。

## 🚀 ArkBar v0.3.6 更新说明

### 🛠 v0.3.6 优化
- TeamoRouter 页面固定展示「账户余额」和「今日费用」；同步完成前显示 `--`，避免旧缓存导致两张卡片消失。
- TeamoRouter 缓存命中率统一四舍五入到 1 位小数（例如 `69.1%`），不再显示冗长小数。

### 🛠 v0.3.5 修复（保留）

### 🛠 v0.3.5 修复
- 修复 TeamoRouter 已配置 API Key 后仍显示「请在设置中填入 API Key」的问题。
  - 根因：HTTP 客户端误将 `curl` 进程退出码 0 判断为非 2xx HTTP 状态，导致成功响应被错误丢弃。
  - 现在 Teamo 成功响应会正常入库并展示余额、当日费用、Token 与缓存命中统计。

### 🌟 v0.3.4 更新说明（保留）

### 🌟 本次版本核心更新：接入 TeamoRouter 余额与用量统计

1. **TeamoRouter 新服务商接入**：
   - 新增 TeamoRouter 标签页，使用官方开放接口实时查询账户余额、今日费用、Token 用量与缓存命中率。
   - 支持近 5 小时 / 今日 / 本周 / 本月多维度 Token 统计，以及月度上下文缓存命中展示。
   - 设置页新增 TeamoRouter API Key 本地保存入口（存储在 `~/.ark-bar/tokens.json`）。
   - 主面板直观展示「账户余额」与「今日费用」两张金额卡片。

### 🌟 0.3.3 更新说明（保留）

1. **Grok / 全厂商用量切换秒级静默更新（彻底免手动刷新）**：
   - 切换顶部服务商标签或从其他窗口唤醒 ArkBar 菜单栏面板时，前端自动以 `force = true` 在后台静默获取最新实时数据。
   - 保证 0 延时平滑切 Tab 的同时，最新用量数百毫秒内平滑就绪，再也不需要手动去点「刷新」按钮。
   - 容错兼容 Grok CLI `onDemandUsed` / `onDemandCap` 结构化对象提取。

2. **上下文缓存 (Prompt Cache) 命中统计全面重构**：
   - **消除误导性的 `--`**：此前 0 命中时会被 JS 判定为 falsy 从而显示为 `--` 导致误以为接口报错；现全面区分厂商能力与真实命中。
   - **火山方舟 & Codex 精准呈现**：当月累计缓存命中量（如月 2.11M）与缓存命中率（如 68.5%）精准呈现；今日有命中高亮显示，今日未命中但本月有命中展示月度累计。
   - **主面板 Token 胶囊展示真实月度命中率**：顶部「本月已消耗 X.XXM Tokens」胶囊右侧准确展示当月整体缓存命中率（如 `⚡ 缓存命中 68.5%`）。
   - **Grok & Antigravity 诚实标注**：官方接口未开放上下文 Prompt Cache 指标的厂商诚实标注为 `暂不支持`。

---

### 🌟 0.3.x 核心特性回顾
- **服务商自由调序与显隐自定义**：主面板支持拖拽排序，设置中心支持微调与隐藏不需要的服务商。
- **检查更新极致优化**：异步非阻塞命令，彻底解决卡顿，解决 WebKit 焦点重置跳回顶部问题，新版本提示置顶。
- **多厂商 Token 详细用量看板**：全面支持近 5 小时、今日、本周、本月 Token 消耗统计，Prompt/Completion/Cache 命中拆解。
- **历史明细报表与数据导出**：过去 14~30 天历史柱状走势图、按日明细表格及 JSON 一键导出。
- **状态栏托盘增强**：支持显示今日 Token（如 `🔥 602K`）、5小时 Token、配额百分比等多种模式。

---

### ⚠️ macOS 首次打开提示“应用已损坏，无法打开”解决办法
由于开源项目未购买苹果商业开发者证书，从浏览器下载后 macOS Gatekeeper 会自动对应用添加隔离标记。  
**解决方法**：打开系统自带「终端」(Terminal.app)，复制并执行以下命令回车即可：
```bash
xattr -dr com.apple.quarantine /Applications/ArkBar.app
```

---

### 📦 安装包下载指引
- **macOS (Apple Silicon M1/M2/M3/M4 系列)**: `ArkBar_*_aarch64.dmg`
- **macOS (Intel 处理器)**: `ArkBar_*_x64.dmg`
- **Windows (64位安装包)**: `ArkBar_*_x64-setup.exe`（或 `.msi`）
