## 🚀 ArkBar v__VERSION__ 更新说明

### 🌟 本次版本更新内容
1. **后台自动同步**：将周期检测从隐藏窗口的 WebView 定时器迁移到 Rust 原生后台线程，菜单栏和悬浮窗在窗口隐藏时也能按设置的频率自动更新。
2. **后台偏好即时生效**：支持运行中切换 5/10/15/30 分钟等检测频率；切换为“仅手动”会暂停自动同步，恢复自动检测后立即补刷一轮。
3. **主面板视觉重构**：以大字号配额 Hero 卡突出已用百分比，重排服务商身份信息、重置倒计时、周期进度与同步状态，优化单套餐和多周期套餐的空间利用率。
4. **设置页视觉重构**：统一区块、行距、控件尺寸和对齐方式，精简下拉选项文案，改进悬浮监控框开关与服务商配置的可读性。

---

### 🛠️ 修复的问题
1. **修复后台检测频率不生效**：解决主窗口隐藏后 WebView 定时器被系统挂起，导致必须点击菜单栏才刷新用量的问题。
2. **修复瞬时网络失败覆盖有效数据**：刷新失败时继续使用最近一次成功的配额数据，避免面板变成未连接、悬浮窗厂商消失或菜单栏百分比被清空。
3. **修复缓存后台刷新可能永久卡住**：后台刷新线程发生异常时会正确释放刷新状态，后续刷新可以继续执行。
4. **修复配额面板信息拥挤与大面积空白**：服务商名称、套餐徽章、账号信息和核心百分比重新分层展示；补充上次同步时间与控制台入口。
5. **修复设置页徽章和布局不一致**：移除无效的小数间距类，统一卡片分隔线、开关、下拉框和版本信息样式。

---

### ⚠️ macOS 首次打开提示“应用已损坏，无法打开”解决办法
由于开源项目未购买苹果商业开发者证书，从浏览器下载后 macOS Gatekeeper 会自动对应用添加隔离标记。  
**解决方法**：打开系统自带「终端」(Terminal.app)，复制并执行以下命令回车即可：
```bash
xattr -dr com.apple.quarantine /Applications/ArkBar.app
```

---

### 📦 安装包下载指引
- **macOS (Apple Silicon M1/M2/M3/M4 系列)**: `ArkBar_*_aarch64.dmg`
- **macOS (Intel 处理器)**: `ArkBar_*_x64.dmg`
- **Windows (64位安装包)**: `ArkBar_*_x64-setup.exe`（或 `.msi`）
